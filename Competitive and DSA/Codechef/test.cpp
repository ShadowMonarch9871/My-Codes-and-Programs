#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    while (n > 0)
    {
        int n, m;
        cin >> n >> m;
        if(m%n==0){
            int div=m/n;
            for(int i=0;i<n;i++){
            if()
            }
        }

        n--;
    }
    // cout<<ceil(100/0.2);
}